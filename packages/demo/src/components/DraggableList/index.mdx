# DraggableList

可拖拽列表

<CodeBlock line={''}>
  <DraggableList
    onDragEnd={(oldIndex, newIndex) => props.reorderItems(oldIndex, newIndex)}
  >
    {props.items.map((item) => {
      return <DraggableItem className="demo-list-item">{item}</DraggableItem>;
    })}
  </DraggableList>
</CodeBlock>
