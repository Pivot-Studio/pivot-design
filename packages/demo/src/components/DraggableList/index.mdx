import demoObj from './demo/index';

# DraggableList

可拖拽模块
``

<CodeBlock line={''}>
  <SortableContext onDragEnd={props.onDragEnd}>
    <DraggableItem className="demo-draggable-item" left={props.coordinates.x} top={props.coordinates.y}>
      draggable
    </DraggableItem>
  </SortableContext>
</CodeBlock>

可拖拽列表

<CodeBlock line={''}>
  <Sortable itemClassName="demo-draggable-item"></Sortable>
</CodeBlock>
