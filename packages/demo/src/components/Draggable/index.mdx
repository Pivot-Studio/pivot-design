# Draggable 拖拽

一个轻量级，高性能的用于 React 的可扩展拖放组件。

目前，针对拖拽组件的开源解决方案多种多样。常见的基于 React 技术栈的拖拽组件有 `react-beautiful-dnd`、`react-sortable-hoc` 以及 `react-dnd` 等。这些组件在不断地迭代改进中，因此它们的使用可能会带来额外的学习成本和包的体积增大，这会让一些开发者望而却步。

这里提供一个比较轻量级的 Draggable 组件。其具有以下特点：

- 零依赖和模块化:库没有外部依赖。它是围绕内置的 React 状态管理和上下文构建的，这保持了库的精简。
- 可拓展性：可供用户自定义事件模型等。
- 灵活性：实现了各种各样的拖拽效果以及拖拽排序。

## 基本示例

下面是一个最基础的拖拽实例，可以用鼠标任意拖拽实例中的元素。

<CodeBlock>
  <Basic />
</CodeBlock>

## 拖拽过程中的元素

拖拽组件支持自定义渲染正在处于拖拽过程中的元素。

<CodeBlock>
  <WithDragOverlay />
</CodeBlock>

## 设置可拖拽区域

支持设置可拖拽的区域。

<CodeBlock>
  <WithHandler />
</CodeBlock>

## 拖放案例

支持拖拽并放置到指定区域

<CodeBlock>
  <Droppable />
</CodeBlock>

## 🌟 拖拽排序

拖拽排序（Drag-and-drop sorting）是一种广泛实用的交互设计手法。支持用户进行垂直、水平以及网格布局的拖拽排序。

### 垂直方向拖拽

<CodeBlock>
  <VerticalSortable />
</CodeBlock>

### 水平方向拖拽

<CodeBlock>
  <HorizenSortable />
</CodeBlock>

### 网格拖拽

<CodeBlock>
  <GridSortable />
</CodeBlock>

### 多容器拖拽（垂直）

支持多个容器之间进行拖拽排序的场景

<CodeBlock style={{ flexWrap: 'nowrap' }}>
  <MultipleContainer />
</CodeBlock>
